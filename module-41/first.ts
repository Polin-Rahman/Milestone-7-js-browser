// Basic typescript type number string boolean
let money: number = 5;
let honey: string = 'omg TS';
let bool: boolean = false;

// function

function add(first: number, second: number): number {
    const result = first + second;
    return result;
}
add(50, 88);

